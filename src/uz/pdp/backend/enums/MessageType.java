package uz.pdp.backend.enums;

/**
 * The MessageType enum represents the types of messages available.
 * It defines two types: CHAT and GROUP.
 */
public enum MessageType {

    CHAT,  // Represents a message sent in a chat
    GROUP; // Represents a message sent in a group
}
